# ReSpeaker Lite — Voice Pipeline
# Python 3.10 recommended
#
# ⚠️  Install in this exact order — version pinning is required
#
# Step 1 — numpy must be < 2 before onnxruntime is installed
numpy<2
#
# Step 2 — onnxruntime 1.17+ causes frozen scores or segfault with openWakeWord
onnxruntime==1.16.3
#
# Step 3 — remaining packages (order does not matter)
pyaudio
sounddevice
faster-whisper
openwakeword
websockets
