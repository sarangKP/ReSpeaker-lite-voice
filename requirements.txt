# Superseded by pyproject.toml — use: uv sync
# Kept as a pip fallback. Install in this order to avoid numpy/onnxruntime conflict:
#
#   pip install "numpy<2"
#   pip install onnxruntime pyaudio faster-whisper openwakeword websockets

numpy<2
onnxruntime
pyaudio
faster-whisper
openwakeword>=0.4
websockets
